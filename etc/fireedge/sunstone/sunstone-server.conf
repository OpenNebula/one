[sunstone]
    # Time configuration mode: browser or os
    # browser: use browser's timezone and locale
    # os: use server's timezone and locale
    time_mode = browser
    
    # Time format: 24h or 12h (used when time_mode = os)
    time_format = 24h
