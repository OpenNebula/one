# Sunstone server configuration
# Timezone configuration: set to a valid timezone string (e.g., "America/New_York")
# or "OS" to use the server's system timezone. If not set or empty, uses browser timezone.
currentTimeZone: OS

# Other configurations...
