# PCI Affinity Scheduler Configuration
# Enable PCI groups antiaffinity scheduling
SCHED_PCI_AFFINITY = "yes"

# Additional PCI scheduling parameters (optional)
# PCI_AFFINITY_STRICT = "yes" # Enforce strict antiaffinity (default: yes)
