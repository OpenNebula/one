---
# This file describes the information and actions available in the DRIVER tab

# Resource

resource_name: "DRIVER"

# Actions - Which buttons are visible to operate over the resources

actions:
  enable: true
  disable: true
  sync: true

# Info Tabs - Which info tabs are used to show extended information

info-tabs:
  info:
    enabled: true
    information_panel:
      enabled: true
  connection:
    enabled: true
  deployment_confs:
    enabled: true
  user_inputs:
    enabled: true        