---
# This file describes the information and actions available in the PROVIDER tab

# Resource

resource_name: "PROVIDER"

# Actions - Which buttons are visible to operate over the resources

actions:
  create_dialog: true
  update_dialog: false
  delete: true
  chown: true
  chgrp: true

# Filters - List of criteria to filter the resources

filters:
  owner: true
  group: true

# Info Tabs - Which info tabs are used to show extended information

info-tabs:
  info:
    enabled: true
    information_panel:
      enabled: true
      actions:
        rename: true
    permissions_panel:
      enabled: true
      actions:
        chmod: true
    ownership_panel:
      enabled: true
      actions:
        chown: true
        chgrp: true

  template:
    enabled: true

# Dialogs

dialogs:
  create_dialog: