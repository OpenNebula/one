terraform {
    required_providers {
        equinix = {
            source = "equinix/equinix"
        }
    }
    required_version = ">= 0.13"
}

provider "equinix" {
  auth_token    = "<%= conn['TOKEN'] %>"
}
