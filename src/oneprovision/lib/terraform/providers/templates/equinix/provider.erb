terraform {
    required_providers {
        packet = {
            source = "packethost/packet"
        }
    }
    required_version = ">= 0.13"
}

provider "packet" {
    auth_token = "<%= conn['TOKEN'] %>"
}

