[vm_search]
# Define searchable template attributes and their matching patterns.
# Each line: <key> = <pattern>
# Supported patterns: exact, prefix, substring. Default exact.
<TEMPLATE ATTRIBUTE 1> = exact
<TEMPLATE ATTRIBUTE 2> = prefix
<TEMPLATE ATTRIBUTE 3> = substring