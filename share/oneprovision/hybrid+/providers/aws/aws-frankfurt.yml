name: 'aws-frankfurt'

provider: 'aws'
provision: 'hybrid+'
description: "LOREM IPSUM"

plain:
  image: 'AWS.webp'

connection:
  access_key: "Encrypted aws access key"
  secret_key: "Encrypted aws secret key"


inputs:
   aws_ami_image:
     - 'ami-6753'
     - 'ami-5654'
   aws_instance_type:
     - 'i3-metal'