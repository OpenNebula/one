{
  "files": [
    {
      "path": "sunstone-server.conf",
      "content": "# Deep VM search configuration\n# List of template attribute keys that can be used for advanced search.\n# Each entry must contain a key path (e.g., TEMPLATE/NAME) and an optional regex pattern.\n# If pattern is omitted, it defaults to '^<search_term>$' (exact match).\n# If pattern includes '.*', it will match prefixes or substrings as needed.\ndeep_search_keys:\n  - key: \"TEMPLATE/NAME\"\n    pattern: \"^.*\"  # free-form substring match (careful with performance)\n  - key: \"TEMPLATE/CPU\"\n    pattern: \"^[0-9]+(\\.[0-9]+)?$\"  # numeric match\n  - key: \"TEMPLATE/MEMORY\"\n    pattern: \"^[0-9]+$\"\n  - key: \"TEMPLATE/SCHED_REQUIREMENTS\"\n    pattern: \".*\"\n  - key: \"USER_TEMPLATE/\"\n    pattern: \"^.*$\""
    },
    {
      "path": "app/controllers/vm_controller.rb",
      "content": "# frozen_string_literal: true\n\nrequire 'opennebula'\n\nclass VMController < ApplicationController\n  # GET /vms\n  def index\n    # Standard query parameters\n    pool = OpenNebula::VirtualMachinePool.new(client)\n    pool.info\n\n    # Apply simple search on table columns\n    vms = pool.select do |vm|\n      match_simple_search(vm)\n    end\n\n    # Apply deep/advanced search if parameter present\n    if params[:deep_search] && !params[:deep_search].empty?\n      vms = vms.select { |vm| match_deep_search(vm, params[:deep_search]) }\n    end\n\n    render json: vms.map { |vm| vm.to_hash }\n  end\n\n  private\n\n  # Simple search by column name (e.g., name, status)\n  def match_simple_search(vm)\n    return true if params[:search].nil? || params[:search].empty?\n\n    query = params[:search].downcase\n    vm.name.downcase.include?(query) ||\n      vm.state_str.downcase.include?(query) ||\n      vm.id.to_s.include?(query)\n  end\n\n  # Deep search: expects a hash of { key => value } where key is a dot-separated path\n  # and value is the search term. The key and pattern are validated against sunstone config.\n  def match_deep_search(vm, deep_params)\n    config = YAML.load_file('sunstone-server.conf')\n    allowed_keys = config['deep_search_keys'] || []\n\n    deep_params.each do |key_path, search_term|\n      # Find matching configuration entry (exact or prefix)\n      config_entry = allowed_keys.find do |entry|\n        key_path.start_with?(entry['key']) || entry['key'] == key_path\n      end\n      next unless config_entry\n\n      # Get the actual value from VM template\n      value = get_vm_attribute(vm, key_path)\n      next if value.nil?\n\n      # Apply the pattern from configuration\n      pattern = config_entry['pattern'] || \"^#{Regexp.escape(search_term)}$\"\n      regex = Regexp.new(pattern, Regexp::IGNORECASE)\n      return false unless value.to_s.match?(regex)\n    end\n    true\n  end\n\n  # Retrieve a nested attribute from VM's TEMPLATE or USER_TEMPLATE\n  # e.g., \"TEMPLATE/CPU\" or \"USER_TEMPLATE/description\"\n  def get_vm_attribute(vm, key_path)\n    parts = key_path.split('/')\n    obj = vm.to_hash\n    parts.each do |part|\n      if obj.is_a?(Hash)\n        obj = obj[part]\n      else\n        return nil\n      end\n    end\n    obj\n  end\nend\n"
    },
    {
      "path": "public/js/sunstone_advanced_search.js",
      "content": "// Advanced search functionality for VM table\n// Adds an "Advanced" dropdown next to the simple search bar\n\n(function() {\n  'use strict';\n\n  // Configuration - should match backend settings\n  var DEEP_SEARCH_KEYS = [\n    { key: 'TEMPLATE/NAME', label: 'VM Name (matches any substring)', pattern: '^.*' },\n    { key: 'TEMPLATE/CPU', label: 'CPU Count', pattern: '^[0-9]+(\\\\.[0-9]+)?$' },\n    { key: 'TEMPLATE/MEMORY', label: 'Memory (MB)', pattern: '^[0-9]+$' },\n    { key: 'USER_TEMPLATE/DESCRIPTION', label: 'Description', pattern: '^.*$' }\n  ];\n\n  var advancedSearchDropdown = null;\n  var advancedSearchFields = {};\n\n  function initAdvancedSearch() {\n    var searchContainer = document.getElementById('vm-search-container');\n    if (!searchContainer) return;\n\n    // Create advanced search button and dropdown\n    var advancedBtn = document.createElement('button');\n    advancedBtn.className = 'btn btn-sm btn-outline-secondary dropdown-toggle';\n    advancedBtn.type = 'button';\n    advancedBtn.innerText = 'Advanced';\n    advancedBtn.setAttribute('data-bs-toggle', 'dropdown');\n    advancedBtn.setAttribute('aria-expanded', 'false');\n\n    var dropdownMenu = document.createElement('div');\n    dropdownMenu.className = 'dropdown-menu dropdown-menu-end p-3';\n    dropdownMenu.style.minWidth = '300px';\n\n    // Build form fields from DEEP_SEARCH_KEYS\n    DEEP_SEARCH_KEYS.forEach(function(config) {\n      var fieldGroup = document.createElement('div');\n      fieldGroup.className = 'mb-2';\n\n      var label = document.createElement('label');\n      label.className = 'form-label small';\n      label.innerText = config.label;\n      label.setAttribute('for', 'deep-search-' + config.key.replace(/\\//g, '-'));\n\n      var input = document.createElement('input');\n      input.type = 'text';\n      input.className = 'form-control form-control-sm deep-search-input';\n      input.id = 'deep-search-' + config.key.replace(/\\//g, '-');\n      input.placeholder = 'Search...';\n      input.dataset.key = config.key;\n\n      fieldGroup.appendChild(label);\n      fieldGroup.appendChild(input);\n      dropdownMenu.appendChild(fieldGroup);\n\n      advancedSearchFields[config.key] = input;\n    });\n\n    // Submit button\n    var applyBtn = document.createElement('button');\n    applyBtn.className = 'btn btn-primary btn-sm w-100 mt-2';\n    applyBtn.innerText = 'Apply';\n    applyBtn.addEventListener('click', function() {\n      applyAdvancedSearch();\n    });\n    dropdownMenu.appendChild(applyBtn);\n\n    var dropdown = document.createElement('div');\n    dropdown.className = 'btn-group';\n    dropdown.appendChild(advancedBtn);\n    dropdown.appendChild(dropdownMenu);\n\n    searchContainer.appendChild(dropdown);\n    advancedSearchDropdown = dropdown;\n  }\n\n  function applyAdvancedSearch() {\n    var params = {};\n    DEEP_SEARCH_KEYS.forEach(function(config) {\n      var input = advancedSearchFields[config.key];\n      if (input && input.value.trim() !== '') {\n        params[config.key] = input.value.trim();\n      }\n    });\n\n    // Get current table URL and add deep_search parameter\n    var tableUrl = new URL(window.location.href);\n    tableUrl.searchParams.delete('deep_search');\n    tableUrl.searchParams.append('deep_search', JSON.stringify(params));\n    window.location.href = tableUrl.toString();\n  }\n\n  // Call init when DOM is ready\n  if (document.readyState === 'loading') {\n    document.addEventListener('DOMContentLoaded', initAdvancedSearch);\n  } else {\n    initAdvancedSearch();\n  }\n})();\n"
    }
  ],
  "summary": "Implemented deep VM search for OpenNebula Sunstone. Added configuration in sunstone-server.conf for allowed search keys with regex patterns. Backend controller (vm_controller.rb) parses new 'deep_search' parameter, validates keys against config, and applies regex matching on VM template attributes. Frontend JavaScript (sunstone_advanced_search.js) adds an 'Advanced' dropdown with input fields for each configured key, constructing a JSON object sent as the 'deep_search' query parameter. This enables searching by arbitrary VM template attributes not limited to table columns."
}