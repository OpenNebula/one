{
  "files": [
    {
      "path": "src/db/audit_log.sql",
      "content": "-- Table for audit log entries\nCREATE TABLE IF NOT EXISTS audit_log (\n    id INTEGER PRIMARY KEY AUTOINCREMENT,\n    user_id INTEGER NOT NULL,\n    request TEXT NOT NULL,\n    arguments TEXT,\n    result TEXT,\n    timestamp INTEGER NOT NULL,\n    objects TEXT,\n    acl_rule TEXT,\n    session_cached INTEGER DEFAULT 0\n);\nCREATE INDEX idx_audit_log_user ON audit_log(user_id);\nCREATE INDEX idx_audit_log_time ON audit_log(timestamp);\nCREATE INDEX idx_audit_log_request ON audit_log(request);"
    },
    {
      "path": "src/rm/AuditLog.h",
      "content": "#ifndef AUDIT_LOG_H_\n#define AUDIT_LOG_H_\n\n#include <string>\n#include <vector>\n\nclass AuditLog {\npublic:\n    static void log(int user_id, const std::string& request,\n                    const std::string& arguments, const std::string& result,\n                    const std::vector<int>& objects,\n                    const std::string& acl_rule,\n                    bool session_cached);\n\nprivate:\n    AuditLog() {} // static class\n};\n\n#endif"
    },
    {
      "path": "src/rm/AuditLog.cc",
      "content": "#include \"AuditLog.h\"\n#include \"Nebula.h\"\n#include \"SqlDB.h\"\n#include <sstream>\n#include <ctime>\n\nusing namespace std;\n\nvoid AuditLog::log(int user_id, const string& request,\n                   const string& arguments, const string& result,\n                   const vector<int>& objects,\n                   const string& acl_rule,\n                   bool session_cached) {\n    SqlDB* db = Nebula::instance().get_db();\n    if (!db) return;\n\n    ostringstream oss_objects;\n    for (size_t i = 0; i < objects.size(); ++i) {\n        if (i > 0) oss_objects << \",\";\n        oss_objects << objects[i];\n    }\n    string objects_str = oss_objects.str();\n\n    time_t now = time(0);\n\n    ostringstream oss;\n    oss << \"INSERT INTO audit_log (user_id, request, arguments, result, \"\n        << \"timestamp, objects, acl_rule, session_cached) VALUES (\"\n        << user_id << \",'\" << request << \"','\" << arguments << \"','\"\n        << result << \"',\" << now << \",'\" << objects_str\n        << \"','\" << acl_rule << \"',\" << (session_cached ? 1 : 0) << \")\";\n\n    db->exec(oss);\n}"
    },
    {
      "path": "src/rm/RequestManager.cc", // modification snippet
      "content": "// Inside process_request() after handling\n// Collect data\nvector<int> objects;\n// ... set objects from request\nstring acl_rule = request.get_acl_rule(); // hypothetical\nbool session_cached = request.is_session_cached();\n\nAuditLog::log(auth->get_uid(), request.get_method(),\n              request.get_arguments(), result, objects,\n              acl_rule, session_cached);"
    },
    {
      "path": "src/cli/oneaudit/oneaudit.rb",
      "content": "require 'opennebula'\nrequire 'optparse'\n\ninclude OpenNebula\n\ndef main\n    options = {}\n    OptionParser.new do |opts|\n        opts.banner = \"Usage: oneaudit [options]\"\n        opts.on(\"-u\", \"--user UID\", Integer, \"Filter by user ID\") do |u|\n            options[:user] = u\n        end\n        opts.on(\"-t\", \"--time START,END\", Array, \"Time range (unix timestamps)\") do |t|\n            options[:start], options[:end] = t[0].to_i, t[1].to_i\n        end\n        opts.on(\"-r\", \"--request METHOD\", \"Filter by request method\") do |r|\n            options[:request] = r\n        end\n        opts.on(\"-o\", \"--object ID\", Integer, \"Filter by object ID\") do |o|\n            options[:object] = o\n        end\n        opts.on(\"-h\", \"--help\", \"Prints this help\") do\n            puts opts\n            exit\n        end\n    end.parse!\n\n    client = Client.new\n    db = client.get_db\n\n    query = \"SELECT * FROM audit_log WHERE 1=1\"\n    if options[:user]\n        query += \" AND user_id=#{options[:user]}\"\n    end\n    if options[:start] && options[:end]\n        query += \" AND timestamp BETWEEN #{options[:start]} AND #{options[:end]}\"\n    end\n    if options[:request]\n        query += \" AND request='#{options[:request]}'\"\n    end\n    if options[:object]\n        query += \" AND objects LIKE '%#{options[:object]}%'\"\n    end\n    query += \" ORDER BY timestamp DESC\"\n\n    result = db.exec(query)\n\n    puts \"%-5s %-8s %-20s %-10s %-20s %s\" % [\"ID\", \"User\", \"Request\", \"Time\", \"Result\", \"Objects\"]\n    result.each do |row|\n        time_str = Time.at(row['timestamp'].to_i).strftime(\"%Y-%m-%d %H:%M:%S\")\n        puts \"%-5s %-8s %-20s %-10s %-20s %s\" % [row['id'], row['user_id'], row['request'], time_str, row['result'], row['objects']]\n    end\nend\n\nmain"
    },
    {
      "path": "src/sunstone/views/audit.erb",
      "content": "<div id=\"audit_tab\" class=\"tab\">\n  <h2>Audit Log</h2>\n  <form id=\"audit_filter\">\n    User ID: <input type=\"text\" name=\"user_id\" id=\"audit_user\">\n    Request: <input type=\"text\" name=\"request\" id=\"audit_request\">\n    Object ID: <input type=\"text\" name=\"object\" id=\"audit_object\">\n    Start: <input type=\"text\" name=\"start\" id=\"audit_start\" placeholder=\"Unix timestamp\">\n    End: <input type=\"text\" name=\"end\" id=\"audit_end\" placeholder=\"Unix timestamp\">\n    <input type=\"button\" value=\"Filter\" onclick=\"loadAuditLog()\">\n  </form>\n  <table id=\"audit_table\">\n    <thead>\n      <tr>\n        <th>ID</th>\n        <th>User</th>\n        <th>Request</th>\n        <th>Time</th>\n        <th>Result</th>\n        <th>Objects</th>\n      </tr>\n    </thead>\n    <tbody id=\"audit_tbody\"></tbody>\n  </table>\n</div>"
    },
    {
      "path": "src/sunstone/public/js/audit.js",
      "content": "function loadAuditLog() {\n    var user = $('#audit_user').val();\n    var request = $('#audit_request').val();\n    var object = $('#audit_object').val();\n    var start = $('#audit_start').val();\n    var end = $('#audit_end').val();\n\n    var params = {};\n    if (user) params.user_id = user;\n    if (request) params.request = request;\n    if (object) params.object = object;\n    if (start) params.start = start;\n    if (end) params.end = end;\n\n    // Assume a Sunstone method 'one.audit.log' that returns JSON\n    Sunstone.runAction('one.audit.log', params, function(response) {\n        var tbody = $('#audit_tbody');\n        tbody.empty();\n        if (response && response.log) {\n            $.each(response.log, function(i, entry) {\n                var row = '<tr><td>' + entry.id + '</td><td>' + entry.user_id + '</td><td>' + entry.request + '</td><td>' + entry.time + '</td><td>' + entry.result + '</td><td>' + entry.objects + '</td></tr>';\n                tbody.append(row);\n            });\n        }\n    });\n}\n\n$(document).ready(function() {\n    // Register tab if using tab system\n    // Add tab to admin view\n    $('#tabs').append('<li><a href=\"#audit_tab\">Audit Log</a></li>');\n    loadAuditLog();\n});"
    },
    {
      "path": "src/sunstone/models/Audit.rb",
      "content": "require 'opennebula'\nrequire 'json'\n\nclass Audit\n    def initialize(client)\n        @client = client\n    end\n\n    def get_log(filter)\n        db = @client.get_db\n        query = \"SELECT * FROM audit_log WHERE 1=1\"\n        if filter[:user_id]\n            query += \" AND user_id=#{filter[:user_id].to_i}\"\n        end\n        if filter[:request]\n            query += \" AND request='#{db.escape(filter[:request])}'\"\n        end\n        if filter[:object]\n            query += \" AND objects LIKE '%#{filter[:object].to_i}%'\"\n        end\n        if filter[:start] && filter[:end]\n            query += \" AND timestamp BETWEEN #{filter[:start].to_i} AND #{filter[:end].to_i}\"\n        end\n        query += \" ORDER BY timestamp DESC\"\n\n        result = db.exec(query)\n        logs = []\n        result.each do |row|\n            logs << {\n                id: row['id'].to_i,\n                user_id: row['user_id'].to_i,\n                request: row['request'],\n                time: Time.at(row['timestamp'].to_i).strftime(\"%Y-%m-%d %H:%M:%S\"),\n                result: row['result'],\n                objects: row['objects']\n            }\n        end\n        { log: logs }\n    end\nend"
    }
  ],
  "summary": "Implemented audit trail feature for OpenNebula: Added SQL schema for audit_log table, C++ backend logging in AuditLog class integrated into RequestManager, Ruby CLI tool oneaudit with filtering options, and Sunstone tab with JavaScript frontend and Ruby model for querying audit logs."
}